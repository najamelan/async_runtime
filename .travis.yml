language: rust
cache   : cargo

rust:
  # - stable
  # - beta
  - nightly

addons:
  firefox: latest
  chrome: stable

install:
  - cargo install --force wasm-pack

script:

  - cargo test

  - wasm-pack test  --firefox --headless
  - wasm-pack test  --chrome  --headless

